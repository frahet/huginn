name: test

on:
  push:
    paths:
      - 'muninn/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - uses: oven-sh/setup-bun@v2
      - run: bun install
        working-directory: muninn
      - run: bun test
        working-directory: muninn
